import {cssClasses as cssClasses_, numbers as numbers_, strings as strings_} from '@material/dialog/constants.js';

export const cssClasses = cssClasses_;

export const numbers = numbers_;

export const strings = {
    ...strings_,
    FORM_SELECTOR: 'form',
};
